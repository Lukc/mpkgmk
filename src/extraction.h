
#ifndef _H_EXTRACTION
#define _H_EXTRACTION

#include "recipe.h"
#include "modules.h"
#include "configuration.h"

void extract(RecipeElement *recipe, Module **modules, Configuration *configuration, Source **sources);

#endif

